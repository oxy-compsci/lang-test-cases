var a = 1; 
var foo = func(){ 
    var a = 2; 
    var inner = func(){
        print a;
        }; 
    ret inner; 
    }; 
foo()(); 

# test for the ability to return a closure from closure, call that closure, and to correctly keep track of closure environments 