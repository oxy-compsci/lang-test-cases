# Testing using both classes
var first = class {
	var one = func(a) {
		print 2 * 2;
	};
};
var second = class {
	var two = func(b) {
		if (b > 3) {
			print b + 4;
		}
	};
};

first().one(2);
second().two(2);